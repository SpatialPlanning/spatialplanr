% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-climate.R
\name{splnr_climate_priorityAreaApproach}
\alias{splnr_climate_priorityAreaApproach}
\title{Run the Climate Priority Area (CPA) Approach}
\usage{
splnr_climate_priorityAreaApproach(
  features,
  metric,
  targets,
  direction,
  percentile = 5,
  refugiaTarget = 1,
  metric_col = "metric"
)
}
\arguments{
\item{features}{An \code{sf} object of conservation features (binary presence/absence).}

\item{metric}{An \code{sf} object with a column named \code{metric}.}

\item{targets}{A \code{data.frame} with columns \code{feature} and \code{target}.}

\item{direction}{\code{1} (higher = more climate-smart) or \code{-1} (lower = more climate-smart).}

\item{percentile}{Numeric (0-100). Defaults to \code{5}.}

\item{refugiaTarget}{Numeric (0-1). Defaults to \code{1}.}

\item{metric_col}{A single character string giving the name of the column in
\code{metric} that contains the climate metric values. Defaults to \code{"metric"} for
backwards compatibility. Use this argument when your climate data column has
a different name (e.g. \code{"sst_trend"}).}
}
\value{
A \code{list} with:
\itemize{
\item \code{Features}: \code{sf} object with \verb{_CS} and \verb{_NCS} columns per feature.
\item \code{Targets}: \code{data.frame} with adjusted targets.
}
}
\description{
\code{splnr_climate_priorityAreaApproach()} implements the Climate Priority Area
approach by splitting conservation features into climate-smart (CS) and
non-climate-smart (NCS) components and adjusting their targets accordingly.
}
\details{
This function orchestrates two steps:
\enumerate{
\item \strong{Preprocessing} via \code{splnr_climate_priorityArea_preprocess()}.
\item \strong{Target Assignment} via \code{splnr_climate_priorityArea_assignTargets()}.
}
}
\examples{
\dontrun{
initial_targets <- dat_species_bin \%>\%
  sf::st_drop_geometry() \%>\%
  colnames() \%>\%
  data.frame() \%>\%
  setNames(c("feature")) \%>\%
  dplyr::mutate(target = 0.3)

CPA_result <- splnr_climate_priorityAreaApproach(
  features      = dat_species_bin,
  metric        = dat_clim,
  targets       = initial_targets,
  direction     = -1,
  percentile    = 5,
  refugiaTarget = 1
)
out_sf_cpa <- CPA_result$Features
targets_cpa <- CPA_result$Targets
}
}
