% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-climate.R
\name{splnr_climate_percentileApproach}
\alias{splnr_climate_percentileApproach}
\title{Run the Percentile Climate-Smart Approach}
\usage{
splnr_climate_percentileApproach(
  features,
  metric,
  targets,
  direction,
  percentile = 35
)
}
\arguments{
\item{features}{An \code{sf} object of conservation features.}

\item{metric}{An \code{sf} object with a column named \code{metric}.}

\item{targets}{A \code{data.frame} with columns \code{feature} and \code{target}.}

\item{direction}{\code{1} or \code{-1}.}

\item{percentile}{Numeric (0-100). Defaults to \code{35}.}
}
\value{
A \code{list} with:
\itemize{
\item \code{Features}: \code{sf} object filtered to climate-smart occurrences.
\item \code{Targets}: \code{data.frame} with adjusted targets.
}
}
\description{
\code{splnr_climate_percentileApproach()} implements the Percentile Approach to
climate-smart conservation planning by filtering features to their most
climate-resilient areas and adjusting targets accordingly.
}
\details{
This function orchestrates two steps:
\enumerate{
\item \strong{Preprocessing} via \code{splnr_climate_percentile_preprocess()}.
\item \strong{Target Assignment} via \code{splnr_climate_percentile_assignTargets()}.
}
}
\examples{
\dontrun{
initial_targets <- dat_species_bin \%>\%
  sf::st_drop_geometry() \%>\%
  colnames() \%>\%
  data.frame() \%>\%
  setNames(c("feature")) \%>\%
  dplyr::mutate(target = 0.3)

Percentile_result <- splnr_climate_percentileApproach(
  features   = dat_species_bin,
  metric     = dat_clim,
  targets    = initial_targets,
  direction  = 1,
  percentile = 35
)
out_sf_percentile  <- Percentile_result$Features
targets_percentile <- Percentile_result$Targets
}
}
